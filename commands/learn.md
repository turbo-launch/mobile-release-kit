---
description: Capture a lesson from this session into the plugin or the project — whichever it belongs to
---

Capture what was just learned so it is not rediscovered later, using the
**capturing-what-you-learned** skill.

The lesson: $ARGUMENTS

If no lesson was given, look back over this session and propose the one or two things that
actually cost time — a failure that took a while to diagnose, a silent gotcha, a step that
turned out to be undocumented. Do not manufacture a lesson from a session that went
smoothly; say nothing was worth keeping.

For each one, follow the skill: apply the layer test, write it to the right place in the
house style, and report where it went. Stage the commit; **do not push**.
