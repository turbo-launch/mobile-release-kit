# dependencies (Playwright is a peer/dev install, not vendored)
node_modules/

# local render output / scratch
out/
*.local.png
.DS_Store

# editor
.vscode/
.idea/

# npm lockfile (deps are loose ranges for a tool/plugin)
package-lock.json
